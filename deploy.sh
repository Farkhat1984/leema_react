#!/bin/bash
# Скрипт быстрого деплоя для leema_react

set -e  # Остановиться при ошибке

echo "🚀 Начинаю деплой leema_react..."

# Цвета для вывода
GREEN='\033[0;32m'
BLUE='\033[0;34m'
RED='\033[0;31m'
NC='\033[0m' # No Color

# Переход в директорию проекта
cd /var/www/leema_react

echo -e "${BLUE}📦 Установка зависимостей (если нужно)...${NC}"
if [ ! -d "node_modules" ]; then
    npm ci
fi

echo -e "${BLUE}🔨 Сборка проекта (без проверки типов)...${NC}"
npm run build:fast

echo -e "${BLUE}🔄 Перезапуск Docker контейнера...${NC}"
docker restart leema-react

echo -e "${BLUE}🌐 Перезагрузка Nginx...${NC}"
sudo nginx -t && sudo systemctl reload nginx

echo -e "${GREEN}✅ Деплой завершен успешно!${NC}"
echo -e "${GREEN}📍 Сайт доступен по адресу: https://www.leema.kz${NC}"
echo ""
echo -e "${BLUE}💡 Совет: Пользователям нужно сделать Hard Refresh (Ctrl+Shift+R)${NC}"
